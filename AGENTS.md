# Soha App 仓库入口

- 本仓负责 Wails App、原生桥接、网络控制和 `frontend/` 中的 App 界面；控制台 Web 源码归属 `soha-web`，服务端协议以 `soha-contracts` 为源。
- 在 OpenSoha 多仓工作区中读取 `../AGENTS.md` 一次；独立克隆时使用本仓规则，不要求初始化相邻仓库或规划工具。
- 实现或实质审查前读取本文件和本次相关原生实现或前端组件；构建与平台打包参考 [Taskfile](Taskfile.yml)、[CI](.github/workflows/ci.yml) 和 [发布流程](.github/workflows/release.yml)，不创建空仓库技能。
- Go 行为改动验证受影响包，主入口为 `GOWORK=off go test ./...`；桌面测试所需的前端产物和平台依赖按 CI 准备。
- 前端检查在 `frontend/` 执行，按改动选择 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`。局部样式不自动触发原生打包。
- 原生构建使用 `wails3 task build`；平台打包、安装、更新和发布变更按对应 CI/发布任务验收。编译通过不代表原生网络、权限、安装或真实设备验收通过。
- 保留认证、凭据保护、原生权限和网络资源清理边界；不手改生成 bindings 或构建产物。
- 文档和技能改动只检查内容、链接与差异；相关代码和环境未变化时复用成功验证，保留用户未提交改动。

## 变更与验收边界

- 先明确任务属于 `frontend/`、Wails 桥接、原生服务、网络控制还是平台打包；核对调用者、需保持的行为和适用验证入口。只加载相关规则，已读未变化内容可复用。
- App 可共享 Soha 产品语言，但 UI 复用本仓真实组件及交互，不复制 soha-web 源码，不把 Web 的管理表格、全局主题或路由约定强加给桌面端。
- 局部 UI 修改保留桥接参数、登录状态、网络操作、事件订阅与清理；共享桥接/原生服务变更检查全部相关调用者和失败/退出路径，不以单页可见结果代表全平台正确。
- 当前实现是现状证据，不是有效需求的替代品。发现产品、协议、平台约束冲突时说明依据，不能以跳过权限、忽略异常或改写基线完成任务。
- 验证记录包含 App/Core/contracts 提交或版本、操作系统/架构、命令与结果；前端模拟、Go 测试、原生构建和真实网络/设备验收分别报告。缺少目标系统、权限或设备的项列为未运行。
- 任务记录不授权真实网络配置、安装更新或发布；跨仓只同步确实受影响的公开契约和消费者，不将局部任务扩大为 App/Cloud/Web 全面重构。
