# Soha App 仓库入口

## 所有权与任务分派

- 本仓负责 Wails App、原生桥接、网络控制和 `frontend/` 中的 App 界面；控制台 Web 源码归属 `soha-web`，服务端协议以 `soha-contracts` 为源。
- 在已确认的 OpenSoha 多仓工作区读取 `../AGENTS.md` 一次；独立克隆使用本仓规则，不要求初始化相邻仓库或规划工具。已有且未变化的规则可复用，交接后缺少关键约束时补读相关部分。
- 实现或实质审查前确定属于 App 前端、Go 桥接、原生服务、网络控制还是平台打包，并读取对应实现、调用者与测试；不为目录对称创建空仓库技能。
- 构建与平台打包读取 [Taskfile](Taskfile.yml)、[CI](.github/workflows/ci.yml) 和 [发布流程](.github/workflows/release.yml)。计划和工具不构成安装、系统权限变更或发布的授权。

## 变更边界

- 前端复用本仓已验证组件与交互；可以参考 Soha 产品语言，但不直接复制 soha-web 的组件导入、路由或构建假设。维护不自动扩展成页面或原生架构重设计。
- 桥接变化同时核对调用参数、事件、生命周期与错误处理，保留认证、凭据保护、原生权限和网络资源清理；不手改生成 bindings。
- 网络、安装或更新变化明确连接/断开、取消、失败恢复和资源所有者。修改共享默认值或公开契约前说明消费者与新增验证范围；公开协议由 contracts 维护。
- 当前实现是现状证据，不自动是目标规范或认可样板。冲突需记录依据，不为了通过检查改变业务预期、关闭保护或扩大任务授权。

## 验证与交付

- Go 行为改动验证受影响包，主入口为 `GOWORK=off go test ./...`；桌面测试的前端产物及平台依赖按 CI 准备。
- 前端检查在 `frontend/` 执行，按改动选择 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`；新增页面还需验证真实接入、权限、空/错状态及实际界面。局部样式不自动触发原生打包。
- 原生构建使用 `wails3 task build`；平台打包、安装、更新和发布按对应完整门禁验收。编译、mock 桥接、实际原生网络/权限和真实设备验收分别报告，不能互相替代。
- 报告实际平台、代码版本、命令，以及通过、失败、跳过、未运行或环境缺失。成功结果只在相关代码、依赖及环境未变化时复用；不降低既有 CI 或更新基线掩盖失败。
- 文档改动只检查内容、链接与差异。保留用户工作树和生成物，仅提交本次所属修改；未请求时不合并 PR、不发布。
